enum FLAGS {
    CARRY           = 0x01, // Bit 0
    ADD_SUBTRACT    = 0x02, // Bit 1
    PARITY_OVERFLOW = 0x04, // Bit 2
    HALF_CARRY      = 0x10, // Bit 4
    ZERO            = 0x40, // Bit 6
    SIGN            = 0x80  // Bit 7
};
